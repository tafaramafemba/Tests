const calculator  = require('./calculator');

describe('addition', () => {
  test('adds numbers', () => {
    expect(this.add(1, 2)).toBe(3);
  });

  // test('is not sour', () => {
  //   expect(myBeverage.sour).toBeFalsy();
  // });
});